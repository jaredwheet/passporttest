// // $(document).ready(function() {
// //   // This file just does a GET request to figure out which user is logged in
// //   // and updates the HTML on the page
// //   $.get("/api/user_data").then(function(data) {
// //     $(".member-name").text(data.username);
// //   });
// // });
// var lastSleepEntry = "";
// $.get("/api/sleepData/" + username, function (req, res) {

// })
// .then (function (data) {
//     for (var i = 0; i < data.length; i++){
//        lastSleepEntry = data[i] 
//     }
//     lastSleepEnd = lastSleepEntry.timeEnd;
//     lastSleepDuration = lastSleepEntry.duration
//     $(".last-nap").text(lastSleepEnd)
//     $(".last-nap-duration").text(lastSleepDuration)

// })

// $.get("/api/eatingData/" + username, function (req, res) {
    
// })

// $.get("/api/vitalsData/" + username, function (req, res) {
    
// })